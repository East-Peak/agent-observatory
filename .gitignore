# dependencies
node_modules

# build output
dist
tsconfig.tsbuildinfo

# test / coverage
coverage

# verifier logs
.verify-logs/

# generated snapshot + report-only attribution status (rebuilt from fixtures / live ccusage)
data/snapshot.json
data/status.json

# raw ccusage captures (regenerated, not committed)
data/fixtures/raw-*

# OS cruft
.DS_Store
